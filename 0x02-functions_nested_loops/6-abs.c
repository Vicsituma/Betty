#include <stdio.h>
#include "main.h"
/**
* _abs - prints absolute value of n integer
* @n: number to check
* Return: always 0
*/
int _abs(int i)
{
	if (i < 0)
		_putchar(i);
	else
		_putchar(i);
	return (0);
}
