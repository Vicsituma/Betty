#include <stdio.h>
/**
 * main - check the code
 *
 * Return: Always 0.
*/
int main(void)
{	
	char name[20];

	printf("Enter your name: ");
	scanf("%s", name);
	printf("Hello, %s\nWelcome to codescracker.com", name);
	getch();

	return (0);
}
